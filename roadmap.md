# Roadmap

## Vision

`my_agent` 的长期方向不是只做一个问答工具，而是做一个“多人物节目化内容生产系统”：用户输入一个问题、链接或热点，系统自动补全背景，选择合适的人物 agent 进行有趣且有观点的讨论，再把讨论过程变成可视化、可配音、可发布、可复盘的数据闭环。

核心链路：

```text
输入问题/链接
-> 背景补全
-> 问题分类
-> 人物选择
-> 多 agent 对话
-> 节目化改写
-> 可视化时间线
-> 角色语音
-> 视频生成
-> 平台发布
-> 数据分析
-> 反哺选题和角色组合
```

## Phase 1: 输入增强与背景补全

目标：解决“问题太短、上下文不够、新事件没有背景”的问题，让 agent 有足够材料讨论。

### 需求

- 当用户输入很短的问题时，系统应该判断是否缺少背景。
- 当用户输入涉及近期事件、热点、人物、公司、政策、行业变化时，系统应该调用 web search 补全背景。
- 当用户输入社媒链接时，优先提取链接正文和评论；如果正文过短或评论不足，再考虑 web search 补充外部背景。
- 背景补全不能直接替代讨论，只作为讨论素材进入 `NormalizedInput`。

### 触发规则建议

- 输入少于 80 字，且不是明确个人问题。
- 包含“最新、最近、今天、昨天、热搜、刚刚、现在、2026、政策、公司、裁员、产品发布”等实时词。
- 包含具体平台链接，但提取正文少于一定字数。
- 用户显式输入“帮我查背景”“结合最新信息”“看看网上怎么说”。

### 输出结构

```text
【用户原始问题】
...

【背景补全】
- 事件概况
- 时间线
- 关键人物/机构
- 争议点
- 已知事实和不确定信息

【讨论任务】
请基于用户问题和背景补全进行多人物讨论...
```

### 风险

- web search 可能带来错误信息，需要保留来源和时间。
- 背景太长会增加 token 成本，需要摘要压缩。
- 涉及时政、医疗、法律、金融等内容时，主持总结要标注不确定性和风险边界。

## Phase 2: Skill Registry 与人物分类

目标：当人物 skill 变多后，系统能自动判断“哪些人物适合讨论这个问题”，而不是靠固定列表。

### 需求

- 每个 skill 需要维护结构化 metadata。
- 问题输入后先做分类，再按分类选择候选人物。
- 允许用户强制指定人物，也允许系统自动推荐。
- 展示“为什么选这些人物”，增加可解释性。

### 建议 metadata

```yaml
id: xin-qingnian
display_name: 新青年
categories:
  - 战略决策
  - 创业产品
  - 社会观察
suitable_for:
  - 创业判断
  - 组织问题
  - 弱势破局
not_suitable_for:
  - 娱乐八卦
  - 纯情绪陪伴
voice_style:
  tone: 直爽、辩证、有湖南青年气质
visual_style:
  avatar_prompt: ...
```

### 选择策略

- 先用关键词和分类做粗筛。
- 再用 LLM 对问题和 skill metadata 做二次匹配。
- 控制角色组合多样性，避免所有人物都从同一角度说话。
- 支持 `--agents` 强制指定，支持 `--max-agents` 控制数量。

### 后续能力

- `list-skills` 展示分类。
- `list-categories` 展示分类和人物。
- 新增 `recommend-agents "问题"` 命令，单独查看推荐人物和理由。

## Phase 3: 节目化可视化与角色语音

目标：把多 agent 讨论从 HTML 记录升级为“可观看、可听”的节目化内容。

### 可视化

- 讨论过程按时间线展示。
- 每个角色有头像、名称、颜色、发言气泡。
- 支持主持人开场、轮次标题、临时总结、最终收束。
- 支持“点名接话”的视觉表现，例如连线、引用卡片、回复对象。

## Phase 3A: 诙谐幽默的节目化对话

目标：把讨论从“正式分析报告”升级为“有观点、有冲突、有梗、有接话的节目现场”，这是内容传播力的核心。

### 为什么重要

- 严肃分析能解决信息问题，但不一定让人愿意看完。
- 多人物 agent 的优势不是每个人都写一段分析，而是人物之间能碰撞、误解、补刀、反问和互相拆台。
- 如果后续要做可视化、配音、视频发布，文本本身必须先有节目感，否则视频只是把报告念出来。
- 诙谐幽默不是为了搞笑而搞笑，而是让观点更好入口，让冲突更容易被观众记住。

### 产品形态

- 新增 `--style analysis|show`。
- 默认 `analysis` 保持当前严肃讨论。
- `show` 进入节目化模式，第一版固定为“圆桌会议/神人会议”形式。
- 后续可扩展：
  - `debate`：更强辩论感。
  - `roast`：更强吐槽感。
  - `commentary`：更像视频解说。
  - `therapy`：更温和陪伴。

### 对话规则

- 每个角色发言更短、更口语化。
- 每轮必须点名回应至少一个人。
- 允许同意一半、反驳一处、补充一层。
- 允许主持人制造轻微戏剧性，例如“这话一出，桌上安静了两秒”。
- 禁止低俗辱骂、人身攻击、歧视和对真实人物的恶意模仿。
- 不牺牲信息质量，每个梗都必须服务观点推进。

### Prompt 改造

agent 发言 prompt 在 `show` 模式下增加：

```text
你在一档多人物圆桌节目中发言。
你的任务不是写分析报告，而是用该人物视角自然接话。
请点名回应上一轮至少一位人物。
可以有轻微吐槽、反问、类比、接梗，但必须围绕问题推进。
输出 80-220 字，像真实节目发言，不要列条目。
```

主持人 prompt 在 `show` 模式下增加：

```text
请像节目主持人一样收束本轮讨论：
- 笑点回收
- 核心争议
- 下一轮追问
语言要有节奏，但不要喧宾夺主。
```

最终总结在 `show` 模式下改为：

```text
本期收场
1. 笑点回收
2. 核心争议
3. 真能带走的建议
```

### HTML 表现

- 页面标题从“多人物视角讨论”切换为“本期圆桌”。
- 每个角色发言卡片显示“接了谁的话”。
- 轮次总结显示为“主持人控场”。
- 可以增加轻量舞台感，但不做复杂动画。

### 验收标准

- 同一个问题在 `analysis` 模式下像分析报告，在 `show` 模式下像多人节目。
- 第二轮开始能看到具体点名接话，而不是“前面几位都说”。
- 发言有趣，但不跑题。
- 主持总结能回收冲突和笑点，同时给出可执行结论。
- 生成结果适合后续直接转成配音视频脚本。

### 结构化脚本

讨论完成后生成一份节目脚本：

```json
{
  "scenes": [
    {
      "type": "host_intro",
      "speaker": "主持人",
      "text": "今天聊一个问题...",
      "duration_hint": 6
    },
    {
      "type": "dialogue",
      "speaker": "新青年",
      "reply_to": "峰哥亡命天涯视角",
      "text": "峰哥刚才说...",
      "emotion": "认真、反驳但克制",
      "duration_hint": 12
    }
  ]
}
```

### 角色语音

- 每个 agent 配置一个 `voice_id` 或 `voice_style`。
- 第一版建议使用非真人克隆的风格化音色，避免授权和侵权风险。
- TTS 输出每条发言的音频片段。
- 音频片段和字幕时间轴对齐。

### 风险

- 有名人物蒸馏 agent 不应使用未经授权的真人声音克隆。
- 角色声音要表达“风格化角色”，不要暗示真人参与。
- 文本转语音会放大不当内容风险，需要在生成音频前增加审核/过滤。

## Phase 4: 视频生成与发布

目标：把可视化讨论自动转成短视频素材，并能生成发布草稿。

### 视频生成

- 输入：结构化节目脚本、角色头像、角色音频、字幕、背景音乐、封面信息。
- 输出：短视频文件、封面图、标题、简介、标签。
- 支持不同平台比例：
  - 抖音：9:16
  - 小红书：3:4 或 9:16
  - B站：16:9 或 9:16
  - 微博：通用横版/竖版

### 视频形式

- 第一版：对话气泡 + 头像 + 字幕 + 音频。
- 第二版：节目演播厅/圆桌布局。
- 第三版：角色 Live2D、表情变化、镜头切换。

### 发布策略

- 第一版建议只生成发布素材和草稿，不直接自动发布。
- 后续再接入平台 API 或浏览器自动化发布。
- 发布前需要用户确认标题、封面、标签和内容。

### 风险

- 各平台对自动发布、爬取、批量操作限制不同。
- 标题和封面有平台审核风险。
- 自动发布需要账号登录态和权限管理，不应明文保存账号密码。

## Phase 5: 作品数据分析与反馈闭环

目标：发布后分析作品表现，反推选题、角色组合和内容形式。

### 需要记录的 metadata

```json
{
  "work_id": "...",
  "platform": "xiaohongshu",
  "topic": "创业判断",
  "source_type": "manual_text",
  "agents": ["新青年", "峰哥亡命天涯视角"],
  "style": "show",
  "rounds": 2,
  "title": "...",
  "cover_style": "...",
  "published_at": "...",
  "video_duration": 90
}
```

### 数据指标

- 播放量
- 完播率
- 点赞
- 收藏
- 评论
- 转发
- 关注转化
- 评论情绪
- 评论中反复出现的问题

### 分析方向

- 哪些话题更容易出数据。
- 哪些人物组合互动更强。
- 哪种开场更能留住用户。
- 哪种标题/封面更有效。
- 哪个平台适合哪类内容。

### 反馈机制

- 自动生成作品复盘。
- 推荐下一批选题。
- 推荐更适合的角色组合。
- 记录“高表现模板”，供后续视频生成复用。

## Priority Recommendation

建议优先级：

1. 输入增强和 web search 背景补全。
2. skill registry 和人物分类。
3. 诙谐幽默的节目化对话。
4. 节目化可视化脚本。
5. 角色语音。
6. 视频生成。
7. 发布草稿。
8. 数据分析。

原因：前两步决定讨论质量；节目化对话决定内容是否有传播力；可视化和语音决定内容形态；视频生成和发布决定生产效率；数据分析决定能不能形成长期增长闭环。

## Near-Term Implementation Plan

### Milestone 1: Background Context

- 新增 `BackgroundContext` 数据结构。
- 新增 `--web-search auto|on|off`。
- 对短输入和实时输入自动补全背景。
- HTML 展示背景来源和摘要。

### Milestone 2: Skill Registry

- 从 `SKILL.md` 解析更多 metadata。
- 增加 `recommend-agents` 命令。
- 在 HTML 中展示人物入选理由。

### Milestone 3: Script Timeline

- 新增 `--style analysis|show`。
- 实现 show 模式的节目化 prompt、主持人控场和最终收场。
- 让轮次临时总结产出“笑点回收 + 核心争议 + 下一轮追问”。
- 输出 `script.json`。
- 每条发言增加 `reply_to_name`、`emotion`、`duration_hint`。
- HTML 改成时间线节目视图。

### Milestone 4: Voice Prototype

- 为每个 agent 配置 `voice_style`。
- 接入一个 TTS provider。
- 生成每条发言的音频文件。
- HTML 支持逐条播放。

### Milestone 5: Video Prototype

- 使用 `script.json` + 音频 + 字幕生成一个竖屏 mp4。
- 生成封面和标题建议。
- 保存 `publish_package/`。

## Open Questions

- web search 默认是否自动开启，还是只在用户加 `--web-search on` 时开启。
- 角色语音是否只用通用风格音色，还是允许用户自己配置授权音色。
- 第一批重点平台是小红书、抖音、微博、知乎还是 B站。
- 数据分析第一版是手动导入数据，还是通过平台接口/爬取自动获取。
- 节目化风格要偏严肃圆桌，还是偏 WeirdoTV 式有梗会议。
